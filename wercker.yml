box: ruby:2.4.0
services:
  - id: mysql
    env:
      MYSQL_ROOT_PASSWORD: password

build:
  steps:
    - bundle-install
    - rails-database-yml
    - script:
        name: setup
        code: bin/setup
    - script:
        name: rails db:create
        code: bundle exec rails db:create
